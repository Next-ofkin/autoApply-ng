[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npx tsc && npx playwright install chromium"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
